export class InteractionPhantomNote {
    static move(x, y, voice) {
        /* let el = document.getElementById("phantomNote");
         if(el == undefined) {
             el = <any> Drawing.circle(x, y, Layout.NOTERADIUS / 2);
             el.setAttribute("id", "phantomNote")
             el.classList.add("phantomNote");
             el.style.fill = voice.color;
         }*/
    }
}
//# sourceMappingURL=InteractionPhantomNote.js.map